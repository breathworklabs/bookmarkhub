[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm clean-install --prefer-offline --no-audit --no-fund --omit=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "true"